# OWASP ZAP Baseline Scan Configuration for CausalFunnel API
# Used in CI for DAST scanning against staging environment

[config]
# Rules to ignore (document why each is acceptable)
10016  # Web Browser XSS Protection Not Enabled — API only, no HTML served
10036  # Server Leaks Version Information — suppressed by helmet
